#!/bin/bash

SOURCE_RELEASE=$1
TARGET_RELEASE=$2

RELEASE_TITLE="## $TARGET_RELEASE"
CHANGELOG="$(git log origin/release/$SOURCE_RELEASE..origin/release/$TARGET_RELEASE --oneline --all --decorate --abbrev-commit)"
FILE_NAME="CHANGELOG.md"

echo -e "$RELEASE_TITLE\n\n$CHANGELOG\n\n"

